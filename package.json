{
  "name": "@potonz/shortlinks-manager-monorepo",
  "author": {
    "name": "Thomas Nguyen",
    "email": "tom@tomng.dev"
  },
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "bun run ./scripts/build.ts",
    "release": "bun run ./scripts/release.js",
    "publish": "bun run ./scripts/publish.ts",
    "lint": "tsc --noEmit && tsc -p packages/shortlinks-manager/tsconfig.json --noEmit && tsc -p packages/shortlinks-manager-cloudflare-d1/tsconfig.json --noEmit && tsc -p packages/shortlinks-manager-mysql/tsconfig.json --noEmit && tsc -p packages/shortlinks-manager-postgres/tsconfig.json --noEmit && eslint --fix .",
    "lint:check": "tsc --noEmit && eslint ."
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "catalog": {
      "date-fns": "^4.1.0"
    }
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@stylistic/eslint-plugin": "^5.9.0",
    "@types/bun": "latest",
    "dts-bundle-generator": "^9.5.1",
    "eslint": "^9.39.2",
    "eslint-plugin-simple-import-sort": "^12.1.1",
    "globals": "^17.0.0",
    "nanospinner": "^1.2.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.51.0"
  },
  "peerDependencies": {
    "bun": "latest"
  }
}
